import 'package:flutter/material.dart';
class RelationShip_Screen extends StatelessWidget {
  const RelationShip_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
