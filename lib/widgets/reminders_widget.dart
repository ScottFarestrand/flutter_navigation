import 'package:flutter/material.dart';
class Reminders_Widget extends StatelessWidget {
  const Reminders_Widget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Reminders"),);
  }
}
