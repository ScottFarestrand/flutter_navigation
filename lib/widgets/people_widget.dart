import 'package:flutter/material.dart';
class People_Widget extends StatelessWidget {
  const People_Widget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("People Widget"),
        );
  }
}
